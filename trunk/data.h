/* obra_info is the structure that will hold
   all the info about each artwork. */
typedef struct {
  char title[200];
  char tipo[100];
  char autor[100];
  int ano;
  float valor;
  char img[7];
} obra_info;
